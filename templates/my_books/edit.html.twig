{% extends 'base.html.twig' %}

{% block title %}Edit Inventory{% endblock %}

{% block body %}
<div class="bg-image" style="background-image: url('https://basecamplive.com/wp-content/uploads/2023/02/books-700x700.jpeg'); height: 100vh">
    <div class="container text-center">
        <h1>Edit</h1>

        {{ include('my_books/_form.html.twig', {'button_label': 'Update'}) }}

        <div class="button-group">
            <a href="{{ path('Inventory_list') }}" class="btn btn-light mt-3 mb-3 btn-sm"> <i class="fas fa-arrow-alt-circle-left"></i> back to list</a>
        {% if is_granted('ROLE_ADMIN') %}
            {{ include('my_books/_delete_form.html.twig') }}
        {% endif %}
        </div>
    </div>
</div>
{% endblock %}
