{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
            <h1>Members List</h1>
                <table class="table">
                    <thead>
                        <tr>
                            <th>Nom du membre</th>
                            <th>Description</th>
                            <th>Détails du membre</th>

                        </tr>
                    </thead>
                    <tbody>
                        {% for membre in membres %}
                            <tr>
                                <td>{{membre.Name}}</td>
                                <td>{{membre.description}}</td>   
                                <td>
                                   <a href="{{ path('Member_show', {'id': membre.id}) }}" class="btn btn-dark text-light btn-sm"><i class="fas fa-eye"></i></a>
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
{% endblock %}
