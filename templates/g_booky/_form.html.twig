
{% block styles %}
<style>
    .form-container {
        width: 30%; 
        margin: 0 auto; 
        padding: 20px; 
        border: 1px solid #ccc; 
        border-radius: 5px; 
        background-color: #f5f5f5; 
    }

    .form-group {
        margin-bottom: 20px; 
    }

    .form-label {
        font-weight: bold; 
    }

    .form-control {
        width: 100%; 
        padding: 10px; 
        border: 1px solid #ccc;
        border-radius: 5px; 
    }

    .btn-save {
        background-color: #007bff;
        color: #fff; 
        border: none; 
        padding: 10px 20px; 
        border-radius: 5px; 
    }
</style>
{% endblock %}

{% block form %}
    <div class="form-container">
        <form method="{{ form.vars.method }}" action="{{ form.vars.action }}" class="custom-form-class">
            {{ form_errors(form) }}

            <div class="form-group">
                {{ form_label(form.GDescription, 'Description', {'label_attr': {'style': 'font-weight: bold;'}}) }}
                {{ form_widget(form.GDescription, {'attr': {'class': 'form-control'}}) }}
            </div>

            <div class="form-group">
                {{ form_label(form.Published, 'Published', {'label_attr': {'style': 'font-weight: bold;'}}) }}
                {{ form_widget(form.Published, {'attr': {'class': 'form-control'}}) }}
            </div>

            <div class="form-group">
                {{ form_label(form.galeries, 'Books', {'label_attr': {'style': 'font-weight: bold;'}}) }}
                {{ form_widget(form.galeries, {'attr': {'class': 'form-control'}}) }}
            </div>

            <div class="form-group">
                {{ form_label(form.member, 'Creator', {'label_attr': {'style': 'font-weight: bold;'}}) }}
                {{ form_widget(form.member, {'attr': {'class': 'form-control'}}) }}
            </div>

            <div class="form-group">
                {{ form_row(form._token) }}
            </div>

            <button class="btn btn-dark btn-sm" type="submit">
                <i class="fas fa-save"></i> {{ button_label|default('Save') }}
            </button>
        </form>
    </div>
{% endblock %}


